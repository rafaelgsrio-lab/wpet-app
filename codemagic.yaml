workflows:
  expo-workflow:
    name: Expo Android Build
    max_build_duration: 60
    environment:
      node: latest
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build Android
        script: npx expo prebuild
    artifacts:
      - build/**/*
